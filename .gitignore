node_modules/
.vs/
.expo/
.expo-android-smoke/
.DS_Store

# Environment variables — never commit the real .env
.env
server/.env

# Keep the example tracked so others know what vars are needed
!.env.example
